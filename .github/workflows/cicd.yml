name: SCA - Revisão de Dependências

on:
  pull_request:
    branches: [ "main" ]

jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Dependency Review
        uses: actions/dependency-review-action@v4
